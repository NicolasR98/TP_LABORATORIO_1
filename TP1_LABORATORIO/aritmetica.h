#include <stdio.h>

/** \brief La funcion suma dos enteros y devuelve un entero.
 *
 * \param int a
 * \param int b
 * \return int r
 *
 */
int suma (int a, int b);

/** \brief La funcion resta dos enteros y devuelve un entero.
 *
 * \param int a
 * \param int b
 * \return int r
 *
 */
int resta (int a, int b);

/** \brief La funcion multiplica dos enteros y devuelve un entero.
 *
 * \param int a
 * \param int b
 * \return int r
 *
 */
int multiplicacion (int a, int b);

/** \brief La funcion divide dos enteros y devuelve un numero flotante.
 *
 * \param int a
 * \param int b
 * \return int r
 *
 */
float division (int a, int b);

/** \brief La funcion saca el factorial de un numero entero y devuelve un entero.
 *
 * \param int a
 * \return int r
 *
 */
int factorial (int a);
